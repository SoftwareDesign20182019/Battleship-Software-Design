
public class PlayerBoard {
	
	private ArrayListyList<Tile> tileList;
	
	public PlayerBoard() {
		for(int numTiles = 0; numTiles < 100; numTiles++) {
			
			Tile newTile = new Tile(numTiles, new EmptyState());
			tileList.add
		}
			
	}

}
